[Unit]
Description=DMS certificates renewal

[Service]
Type=oneshot
ExecStart=docker run --rm -t \
  -v "/opt/mailserver/certbot/certs/:/etc/letsencrypt/" \
  -v "/opt/mailserver/certbot/logs/:/var/log/letsencrypt/" \
  -p 80:80 \
  -p 443:443 \
  certbot/certbot renew
