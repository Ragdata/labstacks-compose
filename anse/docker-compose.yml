# name: Anse (demo)
# description: Lightweight demo frontend for Anse — a local OpenAI-based demo application.
# category: ai
# tags: ai, demo, openai, web
# website: https://github.com/ddiu8081/anse
# docs: https://github.com/ddiu8081/anse#readme
# repo: https://github.com/ddiu8081/anse
---
services:
  anse-demo:
    image: ddiu8081/anse:latest
    restart: always
    expose:
      - 3000
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL}
      - HEAD_SCRIPTS=${HEAD_SCRIPTS}
      - SECRET_KEY=${SECRET_KEY}
      - SITE_PASSWORD=${SITE_PASSWORD}
      - OPENAI_API_MODEL=${OPENAI_API_MODEL}
